/*
 * AddressPage Messages
 *
 * This contains all the text for the AddressPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddressPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddressPage container!',
  },
});
